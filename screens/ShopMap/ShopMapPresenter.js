import React from 'react';
import styled from 'styled-components';

const Container = styled.View`

`;

const Text = styled.Text`

`;

const ShopMapPresenter = ({}) => (
    <Text>
        지도
    </Text>
);

export default ShopMapPresenter;